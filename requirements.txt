# Web stack — PINNED. fastapi was previously unpinned; a deploy resolved to
# fastapi 0.139 + starlette 1.3, an incompatible pair where include_router()
# silently drops sub-routes (only 7 endpoints mounted, no gold/execution). Keep
# fastapi and starlette on a known-compatible pair.
fastapi==0.115.6
starlette==0.41.3
pydantic>=2,<3
uvicorn[standard]>=0.30,<0.35
python-decouple
sqlalchemy>=2,<3
asyncpg
psycopg2-binary
httpx
requests
python-telegram-bot==20.3
cffi>=1.16          # cryptography's binding (python-telegram-bot dep); without it
                    # the rust extension panics on import and can take down boot
redis>=5
polygon-api-client
qrcode[pil]
