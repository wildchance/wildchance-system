# ─────────────────────────────────────────────────────────────────────────────
# Web stack — EXACT pins (boot-verified). An UNPINNED fastapi previously resolved
# to fastapi 0.139 + starlette 1.3, an incompatible pair where include_router()
# silently dropped every router (only 7 endpoints mounted). DO NOT loosen these.
fastapi==0.115.6
starlette==0.41.3
pydantic>=2.9,<3
uvicorn[standard]>=0.30,<0.35

# ─────────────────────────────────────────────────────────────────────────────
# App deps — UPPER-BOUNDED against the next major so a bump can never silently
# drift a deploy (the failure class that took the API down). Patch/minor within
# each bound resolves fine; the ceiling is the guard.
python-decouple>=3.8,<4
sqlalchemy>=2.0,<2.1
asyncpg>=0.29,<0.31
aiosqlite>=0.19,<0.21
psycopg2-binary>=2.9,<3
httpx>=0.24,<0.25            # python-telegram-bot 20.3 requires httpx~=0.24.1
requests>=2.31,<3
python-telegram-bot==20.3
cffi>=1.16,<3               # cryptography's binding (ptb dep) — without it the
                            # rust extension panics on import and can crash boot
redis>=5,<6
polygon-api-client>=1.14,<2
qrcode[pil]>=8,<9
