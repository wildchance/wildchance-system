__pycache__/
*.pyc
*.db
_smoke.db
.pytest_cache/

# Local secrets — keep the template (.env.example) tracked, never the real one.
.env

# Engine output (regenerated by wildchance_scraper.py)
wildchance/feed.json
wildchance/engine.log

# runtime logs
logs/
*.log

# dedup marker state
state/
